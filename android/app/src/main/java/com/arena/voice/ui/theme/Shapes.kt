package com.arena.voice.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Corner-radius scale — tokens.json radius (sm 4, md 6, lg 8, xl 12, xxl 16,
 * full). The web compiles the same scale via Tailwind; pinned by test.
 */
object ArenaRadius {
    val sm = 4.dp
    val md = 6.dp
    val lg = 8.dp
    val xl = 12.dp
    val xxl = 16.dp
    val full = 9999.dp
}
