package com.arena.voice.ui.theme

/**
 * Motion tokens — the shared design system's durations (design/tokens.json,
 * motion.fast_ms = 150, base_ms = 300). Pinned by tests/test_android_design_tokens.py.
 */
object MotionTokens {
    const val FAST_MS = 150
    const val BASE_MS = 300
}
