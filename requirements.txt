# Default developer/CI installation: core runtime plus software-only test tools.
# This intentionally excludes GPU, wake-word, and native audio-driver stacks so
# clean CI can install and run without downloading CUDA or compiling PyAudio.
-r requirements-core.txt
-r requirements-test.txt
