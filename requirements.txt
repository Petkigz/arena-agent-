# Full installation (backward-compatible): core runtime plus every optional
# tool dependency. For a minimal headless runtime, install requirements-core.txt.
-r requirements-core.txt
-r requirements-optional-tools.txt
