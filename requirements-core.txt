# Core API and cognitive runtime. Installing only this file keeps optional
# media, browser, office, speech, and hardware tools unavailable but must not
# prevent CognitiveRuntime startup.
fastapi==0.112.0
uvicorn==0.30.6
websockets>=10.4,<14.0
pydantic==2.8.2
pydantic-settings==2.4.0
httpx==0.27.0
python-multipart>=0.0.9
pyyaml==6.0.2
psutil>=5.9.0
APScheduler>=3.10.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Test runner for the core regression suite.
pytest==8.3.2
pytest-asyncio==0.23.0
